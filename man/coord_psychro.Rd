% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord-psychro.R
\name{coord_psychro}
\alias{coord_psychro}
\title{Advanced psychrometric coordinates}
\usage{
coord_psychro(
  tdb_lim = NULL,
  hum_lim = NULL,
  altitude = NULL,
  units = NULL,
  mollier = NULL,
  expand = FALSE,
  default = TRUE,
  clip = "on"
)
}
\arguments{
\item{tdb_lim}{A numeric vector of length-2 indicating the dry-bulb
temperature limits. Should be in range
\verb{[-50, 100]} degrees C [SI] or
\verb{[-58, 212]} degrees F [IP].
If \code{NULL}, trained data ranges will be used when available, otherwise
a default display range will be used. Default: \code{NULL}.}

\item{hum_lim}{A numeric vector of length-2 indicating the humidity ratio
limits. Should be in range
\verb{[0, 60]} g H2O per kg dry air [SI] or
\verb{[0, 420]} grains H2O per lb dry air [IP].
If \code{NULL}, trained data ranges will be used when available, otherwise
a default display range will be used. Default: \code{NULL}.}

\item{altitude}{A single number of altitude in m [SI] or ft [IP]. If
\code{NULL}, inherits the altitude from the parent \code{\link[=ggpsychro]{ggpsychro()}} plot.}

\item{units}{Unit system, either \code{"SI"} or \code{"IP"}. If \code{NULL}, inherits the
unit system from the parent \code{\link[=ggpsychro]{ggpsychro()}} plot.}

\item{mollier}{If \code{TRUE}, use Mollier chart coordinates. If \code{NULL}, inherits
the chart type from the parent \code{\link[=ggpsychro]{ggpsychro()}} plot.}

\item{expand}{If \code{TRUE}, add a small expansion factor to the limits. Defaults
to \code{FALSE} for psychrometric charts.}

\item{default}{Is this the default coordinate system? Defaults to \code{TRUE} so
replacing the coordinate system created by \code{\link[=ggpsychro]{ggpsychro()}} does not emit a
ggplot2 replacement message.}

\item{clip}{Should drawing be clipped to the extent of the plot panel? A
setting of \code{"on"} (the default) means yes, and a setting of \code{"off"}
means no. In most cases, the default of \code{"on"} should not be changed,
as setting \code{clip = "off"} can cause unexpected results. It allows
drawing of data points anywhere on the plot, including in the plot margins. If
limits are set via \code{xlim} and \code{ylim} and some data points fall outside those
limits, then those data points may show up in places such as the axes, the
legend, the plot title, or the plot margins.}
}
\value{
A ggplot2 coordinate system object for psychrometric charts.
}
\description{
Advanced psychrometric coordinates
}
\details{
Most plots should start with \code{\link[=ggpsychro]{ggpsychro()}}, which installs this coordinate
system and the matching psychrometric metadata for you. Call
\code{coord_psychro()} directly only when you are replacing or configuring the
coordinate system on an existing ggpsychro plot. When \code{altitude}, \code{units}, or
\code{mollier} is \code{NULL}, the value is inherited from the parent plot. Supply these
arguments explicitly when using the coordinate system outside that path.
}
\examples{
ggpsychro() +
    coord_psychro(tdb_lim = c(10, 35), hum_lim = c(0, 25))

ggpsychro(units = "IP", altitude = 1000) +
    coord_psychro(
        tdb_lim = c(50, 100),
        hum_lim = c(0, 140),
        units = "IP",
        altitude = 1000
    )

ggpsychro(mollier = TRUE) +
    coord_psychro(
        tdb_lim = c(0, 50),
        hum_lim = c(0, 30),
        mollier = TRUE
    )
}
